---
phase: 02-auth-design
plan: 06
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03, 02-04, 02-05]
files_modified: [src/app/layout.tsx]
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "Navbar and Footer wrap all pages correctly"
    - "Login flow works: non-@ufpr.br email rejected, valid email receives magic link"
    - "Magic link exchange redirects to /dashboard with session persisted"
    - "Unauthenticated /dashboard access immediately redirects to /login"
    - "Dark mode toggle switches site between light and dark themes"
    - "CACIC yellow (#F5B800) visible in navbar badge, buttons, and card borders"
    - "All pages render without errors on mobile (375px) and desktop (1280px)"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root layout with Navbar and Footer wrapping all pages"
      modified: true
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/navbar.tsx"
      via: "imports Navbar and renders at top"
      pattern: "import.*Navbar"
    - from: "src/app/layout.tsx"
      to: "src/components/layout/footer.tsx"
      via: "imports Footer and renders at bottom"
      pattern: "import.*Footer"

---

<objective>
Integrate Navbar and Footer into the root layout and verify the complete Phase 2 implementation: authentication pipeline, design system, responsive layout, and dark mode all work together end-to-end.

Purpose: Final validation checkpoint. Ensures all Phase 2 components (theme, auth, layout, cards, hero) are properly wired and functional on mobile and desktop.

Output: Root layout with Navbar and Footer integration. Manual verification of complete Phase 2 functionality.
</objective>

<execution_context>
@/c/Users/Windows 11/Documents/Blog-cacic/.planning/phases/02-auth-design/02-RESEARCH.md

Depends on (all prior plans):
- 02-01: Theme configuration and Chakra provider
- 02-02: Authentication pipeline (login, callback, middleware)
- 02-03: Navbar and Footer components
- 02-04: Card components
- 02-05: Hero section

Must verify:
- All components render without errors
- Authentication flow is secure (getUser validation)
- Responsive design works at 375px and 1280px
- Dark mode toggle functions
- Semantic tokens used throughout (no hardcoded colors)
</execution_context>

<context>
@/c/Users/Windows 11/Documents/Blog-cacic/.planning/REQUIREMENTS.md

Phase 2 Goal:
Members can authenticate via Magic Link restricted to @ufpr.br, and every page has the correct CACIC visual identity with dark mode support.

Phase 2 Success Criteria (all must be TRUE):
1. A visitor entering a non-@ufpr.br email on the login page sees a clear error and no magic link is sent.
2. A valid @ufpr.br member receives a magic link, clicks it, and lands on `/dashboard` with session persisted across browser refreshes.
3. Navigating to `/dashboard` while unauthenticated redirects to `/login` immediately.
4. The dark mode toggle switches the site between light and dark themes; CACIC yellow (#F5B800) visible in both.
5. Navbar, Footer, PostCard, EventCard, WorkCard, MemberCard, Hero components render correctly on mobile (375px) and desktop (1280px).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Navbar and Footer into root layout</name>
  <files>src/app/layout.tsx</files>
  <action>
  Update **src/app/layout.tsx** to include Navbar and Footer:

  Replace the entire file with:

  ```typescript
  import type { Metadata } from 'next'
  import { Providers } from '@/components/providers/chakra-provider'
  import { Navbar } from '@/components/layout/navbar'
  import { Footer } from '@/components/layout/footer'
  import { Space_Grotesk, Inter, JetBrains_Mono } from 'next/font/google'

  const spaceGrotesk = Space_Grotesk({
    subsets: ['latin'],
    weight: ['400', '700'],
    variable: '--font-space-grotesk',
  })

  const inter = Inter({
    subsets: ['latin'],
    weight: ['400', '500', '600', '700'],
    variable: '--font-inter',
  })

  const jetbrainsMono = JetBrains_Mono({
    subsets: ['latin'],
    weight: ['400', '500'],
    variable: '--font-jetbrains-mono',
  })

  export const metadata: Metadata = {
    title: 'Blog CACIC',
    description: 'Blog do Centro Acadêmico de Ciências da Computação — UTFPR Santa Helena',
    icons: { icon: '/favicon.ico' },
  }

  export default function RootLayout({
    children,
  }: {
    children: React.ReactNode
  }) {
    return (
      <html
        lang="pt-BR"
        className={`${spaceGrotesk.variable} ${inter.variable} ${jetbrainsMono.variable}`}
        suppressHydrationWarning
      >
        <body>
          <Providers>
            <Navbar />
            {children}
            <Footer />
          </Providers>
        </body>
      </html>
    )
  }
  ```

  Key changes:
  - Import Navbar and Footer components
  - Wrap children with Navbar (before children) and Footer (after children)
  - Both components render inside Providers (so theme applies)
  - Navbar is sticky and visible on all pages
  - Footer visible at bottom of all pages
  - Providers wraps everything to enable theme and dark mode
  </action>
  <verify>
  npm run build succeeds; layout.tsx contains:
  - Navbar import and render before children
  - Footer import and render after children
  - Both inside Providers wrapper
  - Metadata and Google Fonts intact
  - suppressHydrationWarning on html element
  </verify>
  <done>Navbar and Footer integrated into root layout</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Complete Phase 2 implementation:
  - Chakra UI v3 theme with CACIC colors and semantic tokens
  - Google Fonts (Space Grotesk, Inter, JetBrains Mono)
  - Magic Link auth with @ufpr.br domain validation
  - Auth callback and session exchange
  - Middleware protecting /dashboard with getUser() validation
  - Responsive Navbar with dark mode toggle
  - Footer with social links
  - Five card components (PostCard, EventCard, WorkCard, MemberCard, CategoryBadge)
  - Hero section with SplitText animation
  - Root layout with Navbar/Footer wrapping all pages
  </what-built>

  <how-to-verify>
  **Step 1: Local Build and Dev Server**
  ```bash
  npm run build
  npm run dev
  ```
  Verify:
  - Build succeeds with no TypeScript errors
  - Dev server starts at http://localhost:3000
  - No console errors in browser

  **Step 2: Theme and Dark Mode**
  1. Visit http://localhost:3000
  2. Inspect page: Should render with Navbar, content, Footer
  3. Look for dark mode toggle in navbar (top right, moon/sun icon)
  4. Click dark mode toggle
   - Light mode: White background (bg.canvas), black text
   - Dark mode: Black background (#0A0A0A), white text
  5. Refresh page — dark mode preference should persist
  6. Check for CACIC yellow (#F5B800) in navbar CA badge, button hover states

  **Step 3: Authentication Flow (requires Supabase setup)**
  Prerequisites: .env.local must have Supabase credentials filled

  1. Click "Login" button in navbar → lands on /login
  2. Enter non-@ufpr.br email (e.g., test@gmail.com) → submit
   - Should see error: "Use seu e-mail institucional da UFPR (@ufpr.br)"
   - No network request to Supabase
  3. Enter valid @ufpr.br email (e.g., test@ufpr.br) → submit
   - Should redirect to /login/verifique-seu-email
   - Check email (Supabase dashboard → Email tab) for magic link
  4. Click magic link in email → redirect to /auth/callback?code=...
   - Should exchange code and redirect to /dashboard
   - Session cookie visible in devtools → Application → Cookies
  5. Refresh /dashboard → should stay on /dashboard (session persisted)
  6. Open new incognito tab, visit /dashboard → redirects to /login (session not available)

  **Step 4: Responsive Design**
  1. Open Chrome DevTools → Toggle device toolbar
  2. Test at 375px (mobile):
   - Navbar hamburger menu shows (not full links)
   - Dark mode toggle visible
   - Login button in mobile menu
   - Footer stacked vertically
   - All text readable without horizontal scroll
  3. Test at 1280px (desktop):
   - Full navbar links visible (Blog, Eventos, Trabalhos, etc.)
   - Dark mode toggle visible
   - Login button visible on right
   - Footer columns side-by-side
   - All components render without layout shifts

  **Step 5: Component Rendering (Visual Check)**
  1. Hero section: Visit http://localhost:3000
   - Watch title animate on load (SplitText effect)
   - Yellow "Explore o Blog" button visible
   - Text readable in light and dark modes
  2. Cards: Create temporary test page with card components
   - PostCard: Left yellow border visible, category badge rendered, no errors
   - EventCard: Event type icon and date displayed
   - WorkCard: Authors and PDF button visible
   - MemberCard: TiltCard effect works (hover/move to see tilt)
  3. Navbar/Footer:
   - Navbar sticky (scrolls with you, stays at top)
   - Footer at bottom on all pages
   - Colors correct in light and dark modes
   - Social links in footer (GitHub, LinkedIn, Email) clickable

  **Step 6: Security Checks**
  1. Inspect cookies in devtools after login
   - Should see `sb-` prefixed auth cookies (from Supabase)
   - Cookies marked as HttpOnly (secure)
  2. Check localStorage/sessionStorage
   - next-themes color mode preference stored (if dark mode toggled)
  3. Network tab:
   - /api/health endpoint accessible
   - CORS headers present on responses
   - API calls to Supabase show secure connection (https)

  **Expected Results: All must be TRUE**
  - Build succeeds
  - Dev server runs without errors
  - Dark mode toggle works and persists
  - CACIC yellow visible in all relevant places
  - Login form rejects non-@ufpr.br emails
  - Magic link flow works (if Supabase creds configured)
  - Session persists across refresh
  - /dashboard protected (redirects to /login if not authenticated)
  - Responsive layout correct at 375px and 1280px
  - All components render without console errors
  - Navbar sticky, Footer at bottom
  - Hero animation smooth (60fps)
  </how-to-verify>

  <resume-signal>
  When verification is complete, respond with one of:
  - "approved" — All checks passed, Phase 2 ready for closure
  - "issues: {list problems}" — Describe any failures or unexpected behavior
  - "need to fix: {specific components}" — Identify components with problems

  If issues found, Claude will create gap-closure plans to fix them.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 2 Success Criteria (from ROADMAP.md):

✅ **Success Criterion 1:** A visitor entering a non-@ufpr.br email on the login page sees a clear error and no magic link is sent.
  - Verification Task 3, Step 2: Non-@ufpr.br email triggers "Use seu e-mail institucional da UFPR (@ufpr.br)" error
  - Expected: No Supabase network request

✅ **Success Criterion 2:** A valid @ufpr.br member receives a magic link, clicks it, and lands on `/dashboard` with session persisted across browser refreshes.
  - Verification Task 3, Steps 3-5: Magic link flow, code exchange, session persistence
  - Expected: Redirect to /dashboard, session persists on refresh

✅ **Success Criterion 3:** Navigating to `/dashboard` while unauthenticated redirects to `/login` immediately.
  - Verification Task 3, Step 6: Incognito tab test
  - Expected: /dashboard → /login redirect with no content flash

✅ **Success Criterion 4:** The dark mode toggle switches the site between light and dark themes; CACIC yellow (#F5B800) visible in both.
  - Verification Task 2: Dark mode toggle test
  - Expected: Light/dark themes switch, yellow badge and button visible in both

✅ **Success Criterion 5:** Navbar, Footer, PostCard, EventCard, WorkCard, MemberCard, and Hero components render correctly on mobile (375px) and desktop (1280px).
  - Verification Tasks 4-5: Responsive design and component rendering
  - Expected: No layout shifts, all components readable at both breakpoints
</verification>

<success_criteria>
Phase 2 is COMPLETE when:
- All automated tasks (01-05) succeed with zero build errors
- Root layout integrates Navbar and Footer
- Checkpoint human verification confirms:
  - Build succeeds and dev server runs
  - Dark mode toggle works
  - CACIC colors visible in theme
  - Authentication flow secure (domain validation, getUser checking, session persistence)
  - Responsive design correct (375px and 1280px)
  - All components render without errors
  - Session properly persisted via Supabase cookies
</success_criteria>

<output>
After checkpoint approval:
1. Create `.planning/phases/02-auth-design/02-06-SUMMARY.md` (verification results)
2. Update `.planning/ROADMAP.md` to mark Phase 2 complete
3. Create `.planning/STATE.md` entry for next phase
</output>
