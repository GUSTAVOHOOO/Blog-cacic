---
phase: 02-auth-design
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified: [src/components/layout/navbar.tsx, src/components/layout/footer.tsx]
autonomous: true
requirements: [UI-05, UI-06]
user_setup: []

must_haves:
  truths:
    - "Navbar displays CACIC logo, navigation links, dark mode toggle, and login button"
    - "Navbar is sticky and positioned at top of all pages"
    - "Navbar is responsive: hamburger menu on mobile (375px), full links on desktop (1280px)"
    - "Dark mode toggle switches site between light and dark themes"
    - "CACIC yellow (#F5B800) brand color visible in navbar badge and dark toggle"
    - "Footer displays at bottom with black background, links, social icons, and UTFPR-SH credit"
    - "Footer is responsive and readable on mobile and desktop"
  artifacts:
    - path: "src/components/layout/navbar.tsx"
      provides: "Sticky navigation bar with responsive menu"
      exports: ["Navbar"]
      min_lines: 40
    - path: "src/components/layout/footer.tsx"
      provides: "Footer with links, socials, and attribution"
      exports: ["Footer"]
      min_lines: 30
  key_links:
    - from: "src/components/layout/navbar.tsx"
      to: "@chakra-ui/react"
      via: "Box, Flex, Button, Menu, Drawer, ColorModeButton"
      pattern: "from @chakra-ui"
    - from: "src/components/layout/navbar.tsx"
      to: "next/link"
      via: "NextLink component for internal navigation"
      pattern: "NextLink.*href="
    - from: "src/components/layout/footer.tsx"
      to: "react-icons"
      via: "Social media icons (GitHub, LinkedIn, etc.)"
      pattern: "react-icons"

---

<objective>
Create responsive Navbar and Footer components that serve as the layout shell for all pages. The navbar includes dark mode toggle and navigation, the footer includes branding and social links.

Purpose: Establish consistent layout across all pages. These components are reused in the root layout (Phase 3+).

Output: `src/components/layout/navbar.tsx` and `src/components/layout/footer.tsx` with full responsive design and dark mode support.
</objective>

<execution_context>
@/c/Users/Windows 11/Documents/Blog-cacic/.planning/phases/02-auth-design/02-RESEARCH.md

Depends on:
- 02-01: Theme configuration with semantic tokens and dark mode
- 02-02: Login page and authentication (navbar links to /login)

Will be consumed by:
- Root layout in Phase 3 (wraps all pages)
</execution_context>

<context>
@/c/Users/Windows 11/Documents/Blog-cacic/.planning/REQUIREMENTS.md

UI Requirements:
- UI-05: Navbar responsive with logo CACIC, links, yellow "CA" badge, dark mode toggle
- UI-06: Footer with black background, links, social icons, UTFPR-SH credit

Design specs from Phase Plan:
- Navbar: sticky, responsive (hamburger on mobile), dark mode toggle, login button
- Footer: black bg (preto #0A0A0A), semantic tokens, social links
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create responsive Navbar component with dark mode toggle</name>
  <files>src/components/layout/navbar.tsx</files>
  <action>
  Create **src/components/layout/navbar.tsx** — Responsive navigation bar:

  ```typescript
  'use client'
  import {
    Box,
    Container,
    Flex,
    HStack,
    Link as ChakraLink,
    Button,
    Menu,
    MenuButton,
    MenuList,
    MenuItem,
    IconButton,
    useBreakpointValue,
    Badge,
  } from '@chakra-ui/react'
  import { ColorModeButton } from '@chakra-ui/color-mode'
  import { HiMenu, HiX } from 'react-icons/hi'
  import NextLink from 'next/link'
  import { useState } from 'react'

  export function Navbar() {
    const [isOpen, setIsOpen] = useState(false)
    const showMobileMenu = useBreakpointValue({ base: true, md: false }, { fallback: 'md' })

    return (
      <Box
        as="nav"
        bg="bg.canvas"
        borderBottomWidth="1px"
        borderColor="border.default"
        position="sticky"
        top={0}
        zIndex={50}
        boxShadow="sm"
      >
        <Container maxW="container.xl">
          <Flex justify="space-between" align="center" h="16">
            {/* Logo */}
            <ChakraLink
              as={NextLink}
              href="/"
              fontWeight="bold"
              fontSize="lg"
              display="flex"
              alignItems="center"
              gap={2}
              _hover={{ textDecoration: 'none', color: 'brand.500' }}
            >
              <Box as="span" fontSize="2xl" color="brand.500">
                CA
              </Box>
              <Box display={{ base: 'none', sm: 'block' }}>CACIC</Box>
            </ChakraLink>

            {/* Desktop Navigation Links */}
            <HStack
              as="nav"
              spacing={6}
              display={{ base: 'none', md: 'flex' }}
            >
              <ChakraLink
                as={NextLink}
                href="/blog"
                _hover={{ color: 'brand.500' }}
              >
                Blog
              </ChakraLink>
              <ChakraLink
                as={NextLink}
                href="/eventos"
                _hover={{ color: 'brand.500' }}
              >
                Eventos
              </ChakraLink>
              <ChakraLink
                as={NextLink}
                href="/trabalhos"
                _hover={{ color: 'brand.500' }}
              >
                Trabalhos
              </ChakraLink>
              <ChakraLink
                as={NextLink}
                href="/membros"
                _hover={{ color: 'brand.500' }}
              >
                Membros
              </ChakraLink>
              <ChakraLink
                as={NextLink}
                href="/sobre"
                _hover={{ color: 'brand.500' }}
              >
                Sobre
              </ChakraLink>
            </HStack>

            {/* Right: Dark Mode Toggle + Login Button */}
            <HStack spacing={4} display={{ base: 'none', md: 'flex' }}>
              <ColorModeButton />
              <Button
                as={NextLink}
                href="/login"
                bg="brand.500"
                color="black"
                _hover={{ bg: 'brand.600' }}
                size="sm"
                fontWeight="bold"
              >
                Login
              </Button>
            </HStack>

            {/* Mobile Menu Button */}
            {showMobileMenu && (
              <HStack spacing={2}>
                <ColorModeButton />
                <Menu isOpen={isOpen} onClose={() => setIsOpen(false)}>
                  <MenuButton
                    as={IconButton}
                    icon={isOpen ? <HiX /> : <HiMenu />}
                    variant="ghost"
                    size="lg"
                    aria-label="Menu"
                  />
                  <MenuList>
                    <MenuItem as={NextLink} href="/blog">
                      Blog
                    </MenuItem>
                    <MenuItem as={NextLink} href="/eventos">
                      Eventos
                    </MenuItem>
                    <MenuItem as={NextLink} href="/trabalhos">
                      Trabalhos
                    </MenuItem>
                    <MenuItem as={NextLink} href="/membros">
                      Membros
                    </MenuItem>
                    <MenuItem as={NextLink} href="/sobre">
                      Sobre
                    </MenuItem>
                    <MenuItem as={NextLink} href="/login">
                      Login
                    </MenuItem>
                  </MenuList>
                </Menu>
              </HStack>
            )}
          </Flex>
        </Container>
      </Box>
    )
  }
  ```

  Implementation notes:
  - 'use client' (responsive behavior requires client-side breakpoints)
  - Sticky positioning at top with z-index 50
  - Logo with yellow CA badge on left
  - Desktop nav links hidden on mobile, visible on md+ breakpoint
  - Mobile hamburger menu (HiMenu/HiX icons)
  - ColorModeButton for dark mode toggle (visible on all breakpoints)
  - Login button on right
  - All links use semantic tokens: brand.500, border.default, etc.
  - Uses Chakra Flex for responsive layout
  </action>
  <verify>
  npm run build succeeds; navbar.tsx contains:
  - 'use client' at top
  - Box with sticky positioning
  - Logo/branding with yellow CA badge
  - Desktop navigation links
  - Mobile hamburger menu with Menu component
  - ColorModeButton for dark mode
  - Login button linking to /login
  - Responsive Flex layout with breakpoints (base, md)
  </verify>
  <done>Navbar created with responsive design and dark mode toggle</done>
</task>

<task type="auto">
  <name>Task 2: Create Footer component with links and social icons</name>
  <files>src/components/layout/footer.tsx</files>
  <action>
  Create **src/components/layout/footer.tsx** — Footer with branding and socials:

  ```typescript
  'use client'
  import {
    Box,
    Container,
    VStack,
    HStack,
    Link as ChakraLink,
    Text,
    IconButton,
    Divider,
  } from '@chakra-ui/react'
  import { FiGithub, FiLinkedin, FiMail } from 'react-icons/fi'
  import NextLink from 'next/link'

  export function Footer() {
    const currentYear = new Date().getFullYear()

    return (
      <Box as="footer" bg="text.primary" color="bg.canvas" mt={16} pt={12} pb={6}>
        <Container maxW="container.xl">
          <VStack spacing={8} align="stretch">
            {/* Footer Content */}
            <HStack justify={{ base: 'center', md: 'space-between' }} flexWrap="wrap" spacing={8}>
              {/* Column 1: About */}
              <VStack align={{ base: 'center', md: 'start' }} spacing={2}>
                <Text fontWeight="bold" fontSize="lg">
                  CACIC
                </Text>
                <Text fontSize="sm" color="bg.card" maxW="sm">
                  Centro Acadêmico de Ciências da Computação — UTFPR Santa Helena
                </Text>
              </VStack>

              {/* Column 2: Links */}
              <VStack align={{ base: 'center', md: 'start' }} spacing={2}>
                <Text fontWeight="bold" fontSize="sm">
                  Links
                </Text>
                <ChakraLink as={NextLink} href="/blog" fontSize="sm" _hover={{ color: 'brand.500' }}>
                  Blog
                </ChakraLink>
                <ChakraLink as={NextLink} href="/eventos" fontSize="sm" _hover={{ color: 'brand.500' }}>
                  Eventos
                </ChakraLink>
                <ChakraLink as={NextLink} href="/contato" fontSize="sm" _hover={{ color: 'brand.500' }}>
                  Contato
                </ChakraLink>
              </VStack>

              {/* Column 3: Social Icons */}
              <VStack align={{ base: 'center', md: 'start' }} spacing={3}>
                <Text fontWeight="bold" fontSize="sm">
                  Siga-nos
                </Text>
                <HStack spacing={3}>
                  <IconButton
                    as="a"
                    href="https://github.com/cacic-ufpr"
                    target="_blank"
                    rel="noopener noreferrer"
                    icon={<FiGithub />}
                    variant="ghost"
                    size="lg"
                    aria-label="GitHub"
                    _hover={{ color: 'brand.500' }}
                  />
                  <IconButton
                    as="a"
                    href="https://linkedin.com/company/cacic-ufpr"
                    target="_blank"
                    rel="noopener noreferrer"
                    icon={<FiLinkedin />}
                    variant="ghost"
                    size="lg"
                    aria-label="LinkedIn"
                    _hover={{ color: 'brand.500' }}
                  />
                  <IconButton
                    as="a"
                    href="mailto:cacic@ufpr.br"
                    icon={<FiMail />}
                    variant="ghost"
                    size="lg"
                    aria-label="E-mail"
                    _hover={{ color: 'brand.500' }}
                  />
                </HStack>
              </VStack>
            </HStack>

            <Divider borderColor="border.default" />

            {/* Copyright */}
            <VStack spacing={2} textAlign="center">
              <Text fontSize="xs" color="bg.card">
                © {currentYear} Centro Acadêmico de Ciências da Computação
              </Text>
              <Text fontSize="xs" color="bg.card">
                UTFPR — Campus Santa Helena
              </Text>
            </VStack>
          </VStack>
        </Container>
      </Box>
    )
  }
  ```

  Implementation notes:
  - 'use client' for client-side rendering of social icons
  - Black background using text.primary semantic token
  - Light text using bg.canvas semantic token
  - Responsive layout: stacked on mobile, columns on desktop
  - Social icons with external links (GitHub, LinkedIn, Email)
  - Icons hover to yellow (brand.500)
  - Divider for visual separation
  - Copyright footer with dynamic year
  - Uses semantic tokens throughout (no hardcoded colors)
  </action>
  <verify>
  npm run build succeeds; footer.tsx contains:
  - 'use client' at top
  - Box with bg="text.primary" (black background)
  - Links to /blog, /eventos, /contato
  - Social media icon buttons (GitHub, LinkedIn, Email)
  - Responsive layout with HStack/VStack
  - Divider for separation
  - Copyright text with dynamic year
  - All links and icons use semantic tokens or brand.500
  </verify>
  <done>Footer created with responsive design and social links</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Both components created and exported
2. npm run build succeeds with no errors
3. Navbar and Footer render without TypeScript errors
4. Dark mode toggle is visible and functional
5. Responsive behavior works on 375px and 1280px viewports
</verification>

<success_criteria>
- UI-05: Navbar responsive (hamburger on mobile, links on desktop), dark toggle visible, login button present
- UI-06: Footer with black background, navigation links, social icons, UTFPR-SH credit
- Navbar sticky and visible on all pages
- Dark mode toggle switches theme (requires root layout integration in Phase 3)
- All components use semantic tokens (no hardcoded colors)
- npm run build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-design/02-03-SUMMARY.md`
</output>
