---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - .gitignore
  - .env.local.example
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/lib/.gitkeep
  - src/lib/supabase/.gitkeep
  - src/lib/services/.gitkeep
  - src/lib/validations/.gitkeep
autonomous: true
requirements:
  - SETUP-01
  - SETUP-03
  - SETUP-05

must_haves:
  truths:
    - "Running `npm run dev` starts the dev server on localhost:3000 with no TypeScript errors"
    - "The project folder structure matches the specified convention (src/lib/supabase/, services/, validations/)"
    - "The .gitignore excludes .env.local, .mcp.json, and supabase/.temp/"
    - "An .env.local.example documents all required environment variable names"
  artifacts:
    - path: "package.json"
      provides: "Next.js 14+ with App Router, TypeScript, dependencies installed"
    - path: "tsconfig.json"
      provides: "Strict TypeScript config with path aliases"
    - path: "next.config.ts"
      provides: "Base Next.js config (security headers added in Plan 03)"
    - path: ".gitignore"
      provides: "Git exclusion rules including .env.local, .mcp.json, supabase/.temp/"
    - path: ".env.local.example"
      provides: "Template of all required environment variables"
    - path: "src/app/layout.tsx"
      provides: "Root layout (Chakra provider added in Phase 2)"
    - path: "src/lib/"
      provides: "Service library folder structure"
  key_links:
    - from: ".env.local.example"
      to: "src/lib/supabase/"
      via: "NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY env var names"
      pattern: "NEXT_PUBLIC_SUPABASE_URL|SUPABASE_SERVICE_ROLE_KEY"
---

<objective>
Bootstrap the Next.js 14+ App Router project with TypeScript and establish the required folder structure that all subsequent plans depend on.

Purpose: Every other plan in Phase 1 (and the entire project) builds on top of this scaffold. It must exist and build cleanly before any other code is written.
Output: A runnable Next.js project with correct folder conventions, strict TypeScript, and a documented environment variable template.
</objective>

<execution_context>
@C:/Users/Windows 11/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Windows 11/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 14+ project with TypeScript</name>
  <files>
    package.json, tsconfig.json, next.config.ts, .gitignore, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css
  </files>
  <action>
    Run: `npx create-next-app@latest . --typescript --app --src-dir --import-alias "@/*" --no-tailwind --eslint`

    This installs the base Next.js project in the current directory (Blog-cacic/) with:
    - App Router (not Pages Router)
    - TypeScript strict mode
    - src/ directory convention
    - @/* import alias
    - No Tailwind (Chakra UI will be added in Phase 2)
    - ESLint

    After scaffolding, verify tsconfig.json has `"strict": true`. If not, add it under `compilerOptions`.

    Update `src/app/page.tsx` to a minimal placeholder (keep it very simple — just a heading saying "Blog CACIC — em construção"):
    ```tsx
    export default function Home() {
      return (
        <main>
          <h1>Blog CACIC — em construção</h1>
        </main>
      );
    }
    ```

    Update `src/app/layout.tsx` to a minimal root layout with `lang="pt-BR"`:
    ```tsx
    import type { Metadata } from 'next';

    export const metadata: Metadata = {
      title: 'Blog CACIC — UTFPR Santa Helena',
      description: 'Blog do Centro Acadêmico de Ciências da Computação da UTFPR-SH',
    };

    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode;
    }) {
      return (
        <html lang="pt-BR">
          <body>{children}</body>
        </html>
      );
    }
    ```

    Do NOT add Chakra, Tailwind, or any UI library — that is Phase 2.
  </action>
  <verify>
    <automated>cd "C:/Users/Windows 11/Documents/Blog-cacic" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build exits with code 0, no TypeScript errors, Next.js outputs .next/ directory</done>
</task>

<task type="auto">
  <name>Task 2: Establish folder structure and environment variable template</name>
  <files>
    src/lib/supabase/.gitkeep,
    src/lib/services/.gitkeep,
    src/lib/validations/.gitkeep,
    .gitignore,
    .env.local.example
  </files>
  <action>
    Create the required library subdirectories by placing a `.gitkeep` file in each:
    - `src/lib/supabase/` — will hold client.ts, server.ts, admin.ts in Plan 03
    - `src/lib/services/` — will hold DAL functions in Phase 3
    - `src/lib/validations/` — will hold Zod schemas in Phase 3

    Update `.gitignore` (append to the existing create-next-app generated file, do NOT replace it):
    ```
    # Environment secrets
    .env.local
    .env*.local

    # Supabase local dev
    supabase/.temp/

    # MCP config (contains credentials)
    .mcp.json
    ```

    Create `.env.local.example` with all environment variable names documented (values are placeholders):
    ```
    # Supabase — Public (safe for client-side)
    NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
    NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

    # Supabase — Private (server-only, NEVER expose to client)
    SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

    # Upstash Redis — for rate limiting (Phase 3)
    UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io
    UPSTASH_REDIS_REST_TOKEN=your-upstash-token-here

    # App
    NEXT_PUBLIC_APP_URL=http://localhost:3000
    ```

    Also create `.env.local` (git-ignored) with the same keys but empty values so the project runs locally without crashing on missing env vars:
    ```
    NEXT_PUBLIC_SUPABASE_URL=
    NEXT_PUBLIC_SUPABASE_ANON_KEY=
    SUPABASE_SERVICE_ROLE_KEY=
    UPSTASH_REDIS_REST_URL=
    UPSTASH_REDIS_REST_TOKEN=
    NEXT_PUBLIC_APP_URL=http://localhost:3000
    ```
  </action>
  <verify>
    <automated>cd "C:/Users/Windows 11/Documents/Blog-cacic" && ls src/lib/supabase/.gitkeep src/lib/services/.gitkeep src/lib/validations/.gitkeep .env.local.example && grep -q ".env.local" .gitignore && grep -q ".mcp.json" .gitignore && grep -q "supabase/.temp/" .gitignore && echo "OK"</automated>
  </verify>
  <done>All three lib subdirectories exist, .gitignore blocks secrets, .env.local.example documents all env var names</done>
</task>

</tasks>

<verification>
Run `npm run dev` from the project root and confirm:
- Dev server starts at http://localhost:3000 with no errors in terminal
- Browser shows "Blog CACIC — em construção"
- No TypeScript compilation errors in terminal output

Run `npm run build` and confirm clean exit.
</verification>

<success_criteria>
- `npm run build` exits 0 with no TypeScript errors
- `src/lib/supabase/`, `src/lib/services/`, `src/lib/validations/` directories exist
- `.gitignore` contains `.env.local`, `.mcp.json`, `supabase/.temp/`
- `.env.local.example` documents all 6 required environment variables
- `src/app/layout.tsx` has `lang="pt-BR"` and correct metadata
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` summarizing:
- Next.js version installed
- TypeScript config path aliases used
- Folder structure created
- List of env var names documented in .env.local.example
</output>
